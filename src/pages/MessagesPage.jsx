/**
 * MessagesPage.jsx
 * Messaging page — shows all conversations and the active chat thread.
 * TODO (Issue #9): Build conversation sidebar and chat thread with useMessages hook.
 */

export default function MessagesPage() {
  return (
    <div>
      <p>Messages coming soon.</p>
    </div>
  );
}
