/**
 * SellPage.jsx
 * Create listing page — form for sellers to list a new item.
 * TODO: Build full listing creation form with image upload, category, tags, and Price Compare.
 */

export default function SellPage() {
  return (
    <div>
      <p>Create listing form coming soon.</p>
    </div>
  );
}
