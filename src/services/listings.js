/**
 * listings.js
 * Service layer for listing CRUD operations against Supabase.
 * Handles creating, reading, updating, and deleting clothing listings.
 */

// TODO: Implement Supabase queries for listings table
export async function getListings() {
  return [];
}

// Fetches a single listing by ID
export async function getListingById(id) {
  return null;
}

// Creates a new listing in Supabase
// TODO: Sanitize listingData before inserting — reject code-like input (see CLAUDE.md)
export async function createListing(listingData) {
  return null;
}

// Updates an existing listing
// TODO: Sanitize updates before inserting — reject code-like input (see CLAUDE.md)
export async function updateListing(id, updates) {
  return null;
}

// Deletes a listing by ID
export async function deleteListing(id) {
  return null;
}
